export type Props = {
  title: string;
};

export type ProductProps = {
  price: number;
  name: string;
  category1: string;
  category2: string;
};

export type CatalogCardProps = {
  price: number;
  name: string;
};
